<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.linxing.linxing_agent.mapper.ActivityLogMapper">

    <resultMap id="BaseResultMap" type="org.linxing.linxing_agent.entity.ActivityLog">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="action_type" property="actionType"/>
        <result column="target_type" property="targetType"/>
        <result column="target_id" property="targetId"/>
        <result column="details" property="details"/>
        <result column="created_at" property="createdAt"/>
    </resultMap>

    <sql id="BaseColumnList">
        id, user_id, action_type, target_type, target_id, details, created_at
    </sql>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO activity_logs (user_id, action_type, target_type, target_id, details)
        VALUES (#{userId}, #{actionType}, #{targetType}, #{targetId}, #{details}::jsonb)
    </insert>

    <select id="findByUserId" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumnList"/>
        FROM activity_logs
        WHERE user_id = #{userId}
        ORDER BY created_at DESC
    </select>

    <select id="countByUserIdAndActionAndDate" resultType="long">
        SELECT COUNT(*)
        FROM activity_logs
        WHERE user_id = #{userId}
          AND action_type = #{actionType}
          AND created_at >= #{startDate}
          AND created_at &lt; #{endDate}
    </select>

    <select id="countTodayUploads" resultType="long">
        SELECT COUNT(*)
        FROM activity_logs
        WHERE user_id = #{userId}
          AND action_type = 'upload'
          AND DATE(created_at) = CURRENT_DATE
    </select>

    <select id="countTodayQueries" resultType="long">
        SELECT COUNT(*)
        FROM activity_logs
        WHERE user_id = #{userId}
          AND action_type = 'query'
          AND DATE(created_at) = CURRENT_DATE
    </select>

    <delete id="deleteById">
        DELETE FROM activity_logs WHERE id = #{id}
    </delete>

    <delete id="deleteByUserId">
        DELETE FROM activity_logs WHERE user_id = #{userId}
    </delete>

</mapper>
