<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.linxing.linxing_agent.agent.mapper.ChatMessageMapper">

    <resultMap id="BaseResultMap" type="org.linxing.linxing_agent.agent.entity.ChatMessage">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="session_id" property="sessionId"/>
        <result column="parent_id" property="parentId"/>
        <result column="type" property="type"/>
        <result column="content" property="content"/>
        <result column="sources" property="sources"/>
        <result column="nearest_summary_message_id" property="nearestSummaryMessageId"/>
        <result column="created_at" property="createdAt"/>
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO chat_messages (user_id, session_id, parent_id, type, content, sources, nearest_summary_message_id, created_at)
        VALUES (#{userId}, #{sessionId}, #{parentId}, #{type}, #{content},
                #{sources}::jsonb, #{nearestSummaryMessageId}, NOW())
    </insert>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT id, user_id, session_id, parent_id, type, content, sources, nearest_summary_message_id, created_at
        FROM chat_messages WHERE id = #{id}
    </select>

    <select id="selectBySessionId" resultMap="BaseResultMap">
        SELECT id, user_id, session_id, parent_id, type, content, sources, nearest_summary_message_id, created_at
        FROM chat_messages
        WHERE session_id = #{sessionId}
        ORDER BY created_at ASC
    </select>

    <select id="selectLatestBySessionId" resultMap="BaseResultMap">
        SELECT id, user_id, session_id, parent_id, type, content, sources, nearest_summary_message_id, created_at
        FROM chat_messages
        WHERE session_id = #{sessionId}
        ORDER BY created_at DESC
        LIMIT 1
    </select>

    <select id="countBySessionId" resultType="int">
        SELECT COUNT(*) FROM chat_messages WHERE session_id = #{sessionId}
    </select>

    <delete id="deleteByIds">
        DELETE FROM chat_messages WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

    <delete id="deleteBySessionId">
        DELETE FROM chat_messages WHERE session_id = #{sessionId}
    </delete>

    <!-- 点查某消息回溯路径上最近的 summary 节点 id-->
    <select id="selectNearestSummaryId" resultType="java.lang.Integer">
        SELECT nearest_summary_message_id FROM chat_messages WHERE id = #{id}
    </select>

    <!-- 批量更新路径后续消息指向新 summary-->
    <update id="updateNearestSummaryId">
        UPDATE chat_messages
        SET nearest_summary_message_id = #{summaryId}
        WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </update>

    <!-- 更新某消息的 parent_id（summary 落盘后挂载用户消息） -->
    <update id="updateParentId">
        UPDATE chat_messages SET parent_id = #{parentId} WHERE id = #{id}
    </update>

</mapper>
